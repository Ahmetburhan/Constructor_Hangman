var inquirer = require('inquirer');
var Word = require('./Word.js')
function Game() {
    this.selectedWord = ['']
    this.letter = selected.split('')
    this.guesses = 10

    this.grabRandomWord = function(){
//grab word from words.js
    }
this.guess = function () {

    //input letter with inquiree check rto see if inside of letter array
    
}

this.play = function () {

    //display board, shows your guess? maybe show left number of huess
    //check to see if word is guessed completeley if so increment wins
    // if guesses <=0 prompt you suck ;-) incremenr losses (if it is required on documentation)
    
}
}